import sharedVars
import json


def helpText():
	print "Avaliable commands:"
	print "!auth (add|remove|check) : Adds, removes or checks auths, you can only add/remove auths with a level lower than yours."
